//&S-
//&T-
//&D-

/*
 *  filename: semtest4.p
 */

looptest;

// main program
begin
    var i: string;
    var ii: string;

    for i := 1 to 10 do
    begin
        var i: 12;             // error (due to line 16)
        var a: "100";

        for i := 1 to 10 do    // error (due to line 16)
        end do

        begin
            var i: real;       // error (due to line 16)
        end
    end
    end do
end
end looptest
